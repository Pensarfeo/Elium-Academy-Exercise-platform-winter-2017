<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Elium - Exercise Platform
                <% if (view.description) { %>
                        <%= view.description.split(/[\\\/]/).slice(-4, -1).map(spaceDate).join(", ") %>
                <% } else { %>
                        WelcomeMaT
                <% } %>
            </a>
        </div>
    </div>
</nav>