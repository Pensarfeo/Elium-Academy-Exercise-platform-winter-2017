### Turn an string into an array

Write a simple function called ```stringToArray``` to split a string and convert it into an array of words.
Example:

```jsx
stringToArray("Robin Singh") // ["Robin", "Singh"]
```
